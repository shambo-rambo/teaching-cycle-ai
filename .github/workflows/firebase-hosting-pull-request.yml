# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on PR
on: pull_request
permissions:
  checks: write
  contents: read
  pull-requests: write
jobs:
  build_and_preview:
    if: ${{ github.event.pull_request.head.repo.full_name == github.repository }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install dependencies
        run: npm ci
        working-directory: ./frontend
      - name: Clean and build with environment variables  
        run: |
          rm -rf dist
          rm -rf node_modules/.vite
          npm run build
        working-directory: ./frontend
        env:
          VITE_FIREBASE_API_KEY: AIzaSyCS2Xo951MdmkH-IlaTXuHGz35y82tIzxo
          VITE_FIREBASE_PROJECT_ID: learning-cycle-v2
          VITE_FIREBASE_STORAGE_BUCKET: learning-cycle-v2.firebasestorage.app
          VITE_FIREBASE_MESSAGING_SENDER_ID: 940623387842
          VITE_FIREBASE_APP_ID: 1:940623387842:web:7e4911cdcaa70fd4fc2f5b
          VITE_GOOGLE_CLIENT_ID: 708925310405-81ev12htr3c4neq6nan9abkm3oeisn0s.apps.googleusercontent.com
          VITE_API_BASE_URL: https://teaching-cycle-backend-531124404080.us-central1.run.app
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_LEARNING_CYCLE_V2 }}
          projectId: learning-cycle-v2
